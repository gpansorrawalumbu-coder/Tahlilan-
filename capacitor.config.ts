import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.lirboyo.tahlilringkas',
  appName: 'Tahlil Lirboyo',
  webDir: 'dist'
};

export default config;
